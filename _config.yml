# SNSシェア用ページだけをJekyllコレクションとして出力します。
# 既存のHTML/CSS/JSはそのまま静的ファイルとして配信されます。
collections:
  sns_share:
    output: true
    permalink: /share/:name/

defaults:
  - scope:
      path: ""
      type: "sns_share"
    values:
      layout: "sns-share"
